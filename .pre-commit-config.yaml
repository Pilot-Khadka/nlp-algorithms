repos:
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v6.0.0
  hooks:
    - id: trailing-whitespace
    - id: check-added-large-files
    - id: check-docstring-first
    - id: check-json
    - id: check-toml
    - id: check-yaml


# - repo: https://github.com/facebook/pyrefly-pre-commit
#   rev: 0.47.0  # Note: this is the version of the pre-commit hook, NOT the pyrefly version used for type checking
#   hooks:
#     - id: pyrefly-check
#       name: Pyrefly (type checking)
#       entry: env PYTHONPATH= pyrefly check
#       pass_filenames: true  # Recommended to do full repo checks. However, you can change this to `true` to only check changed files
#       language: system  # Use system-installed pyrefly
